import React from "react";
import { Container, Title } from "./Card.styles";

const Card = () => {
	return (
		<Container>
			<Title>DY-03 / Archivar Listas</Title>
		</Container>
	);
};

export default Card;
